describe('dependents', function () {
  
  it("should return an `Array` object");

  it("should return empty `Array` if there isn't any dependents");

  it("should return unique strings in an `Array`");

  describe('if first argument is a vinyl', function () {
    
    it("should return an `Array` object");

    it("should return empty `Array` if there isn't any dependents");

    it("should return unique strings in an `Array`");

  });

});