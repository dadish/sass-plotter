describe('importedBy', function () {
  
  it("returns an `Array` object");

  it("returns an empty `Array` if no file is imported by target");

  it("returns an `Array` of filepaths that are imported by target");

  it("returns an empty `Array` if the target is not i the plot");

  it("returned `Array` contains only unique values");

  it("supports vinyl objects");
  
});